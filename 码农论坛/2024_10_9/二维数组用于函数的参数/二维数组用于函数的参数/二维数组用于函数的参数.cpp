#include <iostream>
using namespace std;

#if 0
int main()
{
	// 整形指针
	int* p;
	// 一维整型指针数组，元素是3个整型指针（p[0]、p[1]、p[2]）。
	int* p[3];
	// 函数p的返回值类型是整型的地址。
	int* p();
	// p是函数指针，函数的返回值是整型。
	int* p(int, int);
	return 0;
}
#endif // 0


