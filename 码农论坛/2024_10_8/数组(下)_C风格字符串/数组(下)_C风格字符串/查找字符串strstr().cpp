#include <iostream>
using namespace std;

#if 0
int main()
{
	char* strstr(const char* str, const char* substr);
	// 功能：检索子串在字符串中首次出现的位置。
	//	返回值：返回字符串str中第一次出现子串substr的地址；如果没有检索到子串，则返回0。
	return 0;
}
#endif // 0