#include <iostream>
using namespace std;

#if 0
int main()
{
	int a[2][2];
	// 语法：数组名[行下标][列下标]
	a[1][1];
	//注意：
	//		二维数组下标也必须是整数，可以是常量，也可以是变量。
	//		合法的行下标取值是：0~(行数 - 1)。
	//		合法的列下标取值是：0~(列数 - 1)。

	return 0;
}
#endif // 0
